applications:
- monasca.persister
classes:
- service.monasca.single
parameters:
  monasca:
    persister:
      enabled: true
      bind:
        app:
          host: ${_param:single_address}
        admin:
          host: ${_param:single_address}
