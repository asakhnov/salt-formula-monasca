applications:
- monasca.notification
classes:
- service.monasca.common
parameters:
  monasca:
    notification:
      enabled: true
      version: ${_param:monasca_version}
      smtp_host: localhost
      email_address: admin@bootstrap.local
